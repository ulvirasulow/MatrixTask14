services:
  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: matrix
      MINIO_ROOT_PASSWORD: matrix123
      MINIO_SERVER_URL: http://localhost:9000
      MINIO_BROWSER_REDIRECT_URL: http://localhost:9001
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - lake

  db:
    image: postgres:15-alpine
    container_name: postgres 
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: your_strong_password
      POSTGRES_USER: postgres 
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./postgres/pg_files:/files 
      - postgres_data:/var/lib/postgresql/data
    networks:
      - lake

  spark-master:
    image: apache/spark:3.5.0
    container_name: spark-master
    user: root
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: ["/opt/spark/sbin/start-master.sh"]
    ports:
      - "8080:8080"
      - "7077:7077"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf
      - spark_events:/spark-events
      - ./notebooks:/home/jovyan/work
    networks:
      - lake

  spark-worker-1:
    image: apache/spark:3.5.2
    container_name: spark-worker-1
    user: root
    depends_on:
      - spark-master
    environment:
      - SPARK_NO_DAEMONIZE=true
      - SPARK_WORKER_CORES=1
      - SPARK_WORKER_MEMORY=1G
    command: ["/opt/spark/sbin/start-worker.sh", "spark://spark-master:7077"]
    ports:
      - "8081:8081"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf
      - spark_events:/spark-events
      - ./notebooks:/home/jovyan/work
    networks:
      - lake

  jupyter:
    image: quay.io/jupyter/pyspark-notebook:x86_64-spark-3.5.2
    container_name: jupyter
    depends_on:
      - spark-master
      - minio
      - db 
    ports:
      - "8888:8888"
      - "4040:4040"
    volumes:
      - ./notebooks:/home/jovyan/work
      - spark_events:/spark-events
    environment:
      - SPARK_MASTER=spark://spark-master:7077
      - PYSPARK_SUBMIT_ARGS=--master spark://spark-master:7077 --packages io.delta:delta-spark_2.12:3.5.0,org.apache.hadoop:hadoop-aws:3.3.4,com.amazonaws:aws-java-sdk-bundle:1.12.262,org.postgresql:postgresql:42.6.0 pyspark-shell
      - CHOWN_EXTRA=/spark-events
      - CHOWN_EXTRA_OPTS=-R
      - NB_UID=1000
      - NB_GID=100
      - GRANT_SUDO=yes
    user: root
    networks:
      - lake

networks:
  lake:

volumes:
  minio_data:
  spark_events:
  postgres_data: